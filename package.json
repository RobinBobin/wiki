{
  "dependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "clean-all": "pnpm --include-workspace-root --recursive clean",
    "prepare": "husky"
  }
}
